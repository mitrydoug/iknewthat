//devcontainer.json
{
  "name": "IKnewThat",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "runArgs": ["--env-file","secrets.env"],
  // Features to add to the dev container. More info: https://containers.dev/features.
  // "features": {},
  "customizations": {
    "vscode": {
      "settings": {},
      "extensions": [
        "nomicfoundation.hardhat-solidity"
      ]
    }
  }
}